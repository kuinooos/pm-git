# 全球碳关税（CBAM/CCA）合规审计多智能体系统 — 技术说明文档

> **版本**: v2.0 | **日期**: 2026-07-11 | **架构模式**: 看板 + 监督者 复合多智能体编排

---

## 一、项目简介

### 核心价值

本系统是一款面向**跨境制造企业、物流企业和供应链咨询机构**的 AI 驱动碳关税合规审计平台。基于多智能体协同工作流，自动完成从**数据采集 → 政策匹配 → 碳排放计算 → 风险分析 → 报告生成**的全流程碳审计。

| 核心能力 | 说明 |
|----------|------|
| 🔍 多源数据接入 | 支持 BOM（物料清单）、TMS（运输管理）、海运提单数据导入 |
| 📜 智能政策匹配 | RAG 混合检索 CBAM/CCA 法规库，自动判定 HS Code 管控品类 |
| 🧮 全链路碳排放计算 | Scope 1（直接）/ Scope 2（电力）/ Scope 3（物流）三范围核算 |
| ⚖️ 合规风险分析 | 对标欧盟 CBAM / 美国 CCA 基准值，生成风险等级和整改建议 |
| ⏸️ 人工审查确认 | Human-in-the-Loop 机制，关键节点暂停等待人工审批 |
| 📝 审计报告自动化 | 生成 Markdown + PDF 双格式审计报告 |

### 技术特色

| 特色 | 说明 |
|------|------|
| 🏗️ 复合多智能体编排 | Router（路由分诊）+ Blackboard（看板共享）+ Supervisor（监督者仲裁）三层复合 |
| 🔄 动态监督者循环 | Supervisor 扫描看板状态，按需调度 Agent，非固定流水线 |
| 🎯 意图智能分流 | Router 识别闲聊/政策查询/碳审计，非审计请求不启动工作流 |
| 🧠 混合 RAG 检索 | Milvus 向量检索 + BM25 关键词 + RRF 融合 + BGE-Reranker 重排序 |
| 🔌 优雅降级 | Redis 不可用时自动降级（缓存跳过、Celery 禁用），核心功能不受影响 |
| 🐳 容器化就绪 | Docker Compose 一键部署，app/Redis/Celery/Chainlit 四种 profile |

---

## 二、技术栈

### 前端

| 技术 | 版本 | 用途 |
|------|------|------|
| Chainlit | >= 2.11.0 | 对话式 AI 交互界面，步骤可视化 + 审批按钮 |
| Python | 3.10+ | 前端逻辑语言（Chainlit 基于 Python） |

### 后端

| 技术 | 版本 | 用途 |
|------|------|------|
| Python | 3.10+ | 主开发语言 |
| FastAPI | >= 0.110.0 | REST API 框架，Swagger 自动文档 |
| Uvicorn | >= 0.29.0 | ASGI 异步服务器 |
| Pydantic | >= 2.0.0 | 请求/响应数据模型校验 |
| LangGraph | >= 0.2.39 | 多智能体工作流编排（StateGraph + HITL） |
| LangChain | >= 0.3.0 | LLM 应用框架（Prompt / Chain / OutputParser） |
| LangChain-OpenAI | >= 0.2.0 | OpenAI 兼容 LLM 客户端 |
| LangChain-HuggingFace | >= 0.1.0 | HuggingFace Embeddings 模型加载 |
| LangChain-Community | >= 0.3.0 | BM25 检索器、PDF/TXT 文档加载器 |
| LangChain-Text-Splitters | >= 0.3.0 | RecursiveCharacterTextSplitter 文档分块 |
| LangGraph-Checkpoint-SQLite | >= 2.0.0 | 工作流状态持久化（HITL 断点恢复） |
| LongCat-2.0 | - | LLM 推理模型（OpenAI 兼容 API） |
| PyMilvus | >= 2.4.0 | Milvus Lite 嵌入式向量数据库 |
| Sentence-Transformers | >= 3.0.0 | BGE Embedding + BGE-Reranker 模型 |
| pypdf | >= 4.0.0 | PDF 政策文档解析 |
| fpdf2 | >= 2.8.0 | PDF 审计报告渲染 |
| markdown | >= 3.6 | Markdown 解析 |
| python-multipart | >= 0.0.9 | FastAPI 文件上传 |

### 基础设施

| 技术 | 版本 | 用途 |
|------|------|------|
| Redis | 7.x | RAG 缓存 + Celery Broker + 速率限制 |
| Celery | >= 5.3.0 | 异步碳审计任务队列 |
| PyJWT | >= 2.8.0 | JWT Bearer Token 认证 |
| SQLite | stdlib | LangGraph 检查点 + Milvus Lite 底层存储 |
| JSON | - | 碳排放因子数据库 + HS Code 映射表 |
| Docker | - | 容器化部署 |
| Docker Compose | 3.9 | 多服务编排 |
| LangSmith | >= 0.1.0 | LLM 链路追踪（可选） |
| Fonts Noto CJK | - | Docker 镜像中文字体支持 |

---

## 三、系统架构设计

### 3.1 整体架构图

```mermaid
graph TB
    subgraph 用户层
        A["Chainlit 对话 UI<br/>Port 8000"]
        B["Swagger API 文档<br/>Port 8008/docs"]
    end

    subgraph 接入层["接入层 — FastAPI"]
        C["Router / 意图分流"]
        D["JWT 认证中间件"]
        E["CORS 中间件"]
    end

    subgraph 编排层["多智能体编排层 — LangGraph"]
        F["🧭 Router Agent"]
        G["👁️ Supervisor Agent"]
        H["🔍 信息萃取 Agent"]
        I["📜 政策判定 Agent"]
        J["🧮 排放计算 Agent"]
        K["⚖️ 合规风控 Agent"]
        L["📝 报告生成 Agent"]
        M["⏸️ 人工审查 HITL"]
    end

    subgraph 领域层["领域服务层"]
        N["CarbonCalculator<br/>碳排放引擎"]
        O["CarbonPolicyRAG<br/>RAG 检索引擎"]
        P["ReportRenderer<br/>Markdown → PDF"]
    end

    subgraph 数据层["数据访问层"]
        Q[("Milvus Lite<br/>向量数据库")]
        R[("SQLite<br/>检查点持久化")]
        S[("Redis<br/>缓存 + 消息队列")]
        T[("carbon_db.json<br/>排放因子库")]
        U[("hs_code_mapping.json<br/>HS Code 映射")]
    end

    subgraph 知识源["知识库"]
        V["CBAM 法规摘要"]
        W["ISO 14064 标准"]
        X["美国 CCA 摘要"]
    end

    A --> C
    B --> C
    C --> D --> E --> F
    F -->|chat| A
    F -->|policy_query| O
    F -->|carbon_audit| G
    G --> H & I & J & K & L
    H & I & J & K & L --> G
    G --> M
    M --> L
    H --> N
    I --> O
    L --> N & O & P
    O --> Q & V & W & X
    Q --> S
    N --> T
    I --> U
    M --> R
```

### 3.2 Agent 编排流程图（看板 + 监督者）

```mermaid
stateDiagram-v2
    [*] --> Router

    state Router {
        [*] --> 分析用户意图
        分析用户意图 --> Chat: 问候/闲聊
        分析用户意图 --> PolicyQuery: 政策查询
        分析用户意图 --> CarbonAudit: 碳审计请求
    }

    Chat --> [*]: 直接 LLM 回复
    PolicyQuery --> RAG检索: 调用 RAG 引擎
    RAG检索 --> [*]: 返回政策条文

    CarbonAudit --> Supervisor: 进入看板循环

    state Supervisor {
        [*] --> 扫描看板状态
        扫描看板状态 --> 数据萃取检查: clean_data 为空?
        扫描看板状态 --> 政策匹配检查: policy_info 为空?
        扫描看板状态 --> 排放计算检查: total_emissions 为 0?
        扫描看板状态 --> 合规分析检查: compliance_risk 为空?
        扫描看板状态 --> 报告生成检查: 数据齐备?

        数据萃取检查 --> 指派Agent: 派 data_cleansing
        政策匹配检查 --> 指派Agent: 派 policy_matcher
        排放计算检查 --> 指派Agent: 派 calculator
        合规分析检查 --> 指派Agent: 派 compliance_audit
        报告生成检查 --> 完成判定: approved=true?
    }

    state 专家Agent {
        读取看板输入 --> 执行领域计算
        执行领域计算 --> 写入看板输出
        写入看板输出 --> 回到Supervisor
    }

    指派Agent --> 专家Agent
    回到Supervisor --> 扫描看板状态
    完成判定 --> HumanReview: 等待人工审查
    HumanReview --> 报告生成: approved=true
    HumanReview --> [*]: approved=false (驳回)
    报告生成 --> [*]: 生成 PDF 报告

    note right of Supervisor
        最多循环 3 次
        超限后强制终止
    end note

    note right of 专家Agent
        微观层面：
        每个 Agent 内部
        采用 Plan-then-Execute
    end note
```

### 3.3 RAG 检索引擎流程图

```mermaid
flowchart LR
    subgraph 离线入库["📥 离线入库"]
        A1["PDF/TXT 文档"] --> A2["PyPDFLoader / TextLoader"]
        A2 --> A3["RecursiveCharacterTextSplitter<br/>chunk=500, overlap=100"]
        A3 --> A4["BGE-small-zh-v1.5<br/>HuggingFaceEmbeddings"]
        A4 --> A5[("Milvus Lite<br/>向量库 COSINE")]
        A3 --> A6[("BM25 索引")]
    end

    subgraph 在线检索["🔍 在线检索"]
        B1["用户查询: CBAM 铝制品关税"] --> B2["BGE Embedding<br/>查询向量化"]
        B2 --> B3["Milvus 向量检索<br/>Top-10"]
        B1 --> B4["BM25 关键词检索<br/>Top-10"]
        B3 --> B5{"RRF 融合<br/>Reciprocal Rank Fusion<br/>k=60"}
        B4 --> B5
        B5 --> B6["候选集 Top-20"]
        B6 --> B7["BGE-Reranker<br/>CrossEncoder 精筛"]
        B7 --> B8["最终结果 Top-5"]
    end

    subgraph 缓存层["⚡ 缓存层"]
        B8 --> C1{"Redis 缓存?"}
        C1 -->|命中| C2["直接返回<br/>TTL=3600s"]
        C1 -->|未命中| C3["写入缓存<br/>异步"]
        C2 --> C4["LLM Prompt 上下文"]
        C3 --> C4
    end
```

### 3.4 数据流与存储架构

```mermaid
flowchart TB
    subgraph 输入["输入层"]
        I1["Chainlit 用户自然语言"]
        I2["FastAPI REST API<br/>BOM/TMS JSON"]
    end

    subgraph 处理["处理层"]
        P1["Router: 意图识别"]
        P2["Data Cleansing: LLM 结构化提取"]
        P3["Policy Matcher: HS Code 查询 + RAG"]
        P4["Calculator: 排放因子计算"]
        P5["Compliance Audit: LLM 风险分析"]
        P6["Report Writer: 模板填充"]
    end

    subgraph 存储["存储层"]
        S1[("Milvus Lite<br/>collection: carbon_policy<br/>dim=512, metric=COSINE")]
        S2[("SQLite<br/>checkpoints.db<br/>LangGraph 状态持久化")]
        S3[("Redis<br/>key: rag:{md5(query,top_k)}<br/>TTL=3600s")]
        S4[("JSON 文件<br/>carbon_db.json<br/>hs_code_mapping.json")]
        S5[("文件系统<br/>data/raw_documents/<br/>data/reports/")]
    end

    subgraph 输出["输出层"]
        O1["Chainlit 对话消息"]
        O2["审计报告 Markdown"]
        O3["审计报告 PDF"]
    end

    I1 --> P1
    I2 --> P1
    P1 -->|chat| O1
    P1 -->|policy_query| P3
    P1 -->|carbon_audit| P2
    P2 --> P3 --> P4 --> P5 --> P6
    P2 -.-> S2
    P3 --> S1 & S3 & S4
    P4 --> S4
    P3 -.-> S2
    P4 -.-> S2
    P5 -.-> S2
    P6 --> O2 --> O3
    O2 --> S5
    O3 --> S5
    S1 --> S1
```

### 3.5 数据模型 ER 关系图

```mermaid
erDiagram
    AuditState ||--o{ TransportLeg : contains
    AuditState ||--o{ EnergyConsumption : contains
    AuditState ||--o{ MaterialInput : contains
    AuditState ||--|| EmissionsResult : generates
    AuditState }o--|| HS_CODE_MAPPING : references
    AuditState }o--|| CARBON_DB : references
    AuditState }o--|| RAG_DOCUMENT : retrieves

    AuditState {
        string raw_payload "BOM/TMS 原始数据"
        string target_country "EU/US"
        string route_intent "chat|policy_query|carbon_audit"
        string hs_code "海关编码"
        string product_name "产品名称"
        float product_weight_tons "产品重量"
        string material_type "材料类型枚举"
        list transport_legs "运输段列表"
        float electricity_mwh "电力消耗"
        string cbam_category "管控品类"
        float total_emissions "总排放量"
        string risk_level "low/medium/high"
        string supervisor_decision "next|done|error|pending_review"
        int retry_count "重试计数"
        bool approved "人工审批标志"
        string audit_report_md "审计报告"
    }

    TransportLeg {
        string transport_type "运输方式枚举"
        float weight_tons "重量"
        float distance_km "距离"
        string description "路段描述"
    }

    EmissionsResult {
        float scope1_emissions "直接排放"
        float scope2_emissions "电力间接"
        float scope3_emissions "物流运输"
        float total_emissions "总排放"
        float cbam_taxable_emissions "应纳税排放"
        float cbam_estimated_tax "预估碳关税"
    }

    HS_CODE_MAPPING {
        string hs_code "HS 编码"
        string hs_description "商品描述"
        string cbam_category "CBAM 品类"
        string cca_category "CCA 品类"
        bool cbam_subject "CBAM 管控"
        bool cca_subject "CCA 管控"
    }

    CARBON_DB {
        json transport_factors "运输排放因子"
        json energy_factors "能源排放因子"
        json material_factors "材料排放因子"
        json cbam_benchmarks "CBAM 基准值"
        json port_distances "港口距离"
    }

    RAG_DOCUMENT {
        string content "文档内容"
        string source_file "来源文件名"
        vector embedding "512维向量"
        float score "检索得分"
    }
```

### 3.6 Docker 部署架构图

```mermaid
flowchart TB
    subgraph 宿主机["🖥️ 宿主机"]
        subgraph 网络["Docker Network"]
            subgraph redis服务["Redis Service"]
                REDIS["Redis 7 Alpine<br/>Port: 6379<br/>Volume: redis_data"]
            end

            subgraph app服务["App Service (Default)"]
                APP["FastAPI + Uvicorn<br/>Port: 8008:8008<br/>Volumes: ./data, ./src, ./config"]
            end

            subgraph celery服务["Celery Worker (profile: worker)"]
                CELERY["Celery Worker<br/>concurrency=2<br/>Volumes: ./data, ./src"]
            end

            subgraph chainlit服务["Chainlit UI (profile: chainlit)"]
                CHAINLIT["Chainlit Frontend<br/>Port: 8009:8008<br/>Volumes: ./app.py, ./chainlit.md"]
            end
        end

        subgraph 持久化["💾 持久化卷"]
            V_REDIS[("redis_data<br/>Redis 持久化")]
            V_DATA[("./data<br/>Milvus + SQLite + JSON<br/>绑定挂载")]
        end
    end

    APP --> REDIS
    CELERY --> REDIS
    CHAINLIT --> REDIS
    REDIS --> V_REDIS
    APP --> V_DATA
    CELERY --> V_DATA
    CHAINLIT --> V_DATA

    APP -.->|depends_on healthy| REDIS
    CELERY -.->|depends_on healthy| REDIS
    CHAINLIT -.->|depends_on healthy| REDIS
```

**Profile 说明**:

| 命令 | 启动服务 | 适用场景 |
|------|----------|----------|
| `docker compose up -d` | app + redis | 纯后端 API |
| `docker compose --profile worker up -d` | app + redis + celery | 异步任务 |
| `docker compose --profile chainlit up -d` | chainlit + redis | 前端对话 UI |
| `docker compose --profile prod --profile worker up -d` | 全部服务 | 生产全栈 |

---

## 四、项目结构总览

```
Project Root/
│
├── frontend/                          # 前端 — Chainlit 对话 UI
│   ├── app.py                         # 主入口（Router + 工作流 + 审批回调）
│   ├── chainlit.md                    # 欢迎页 Markdown
│   └── .chainlit/
│       └── config.toml                # Chainlit 配置
│
├── backend/                           # 后端 — FastAPI + LangGraph
│   ├── main.py                        # ⭐ 主入口（uvicorn 启动）
│   │
│   ├── config/
│   │   └── settings.py                # 全局配置（LLM/DB/Redis/JWT/Celery）
│   │
│   ├── src/
│   │   ├── api/                       # 🌐 接入层（FastAPI）
│   │   │   ├── app.py                 # 应用工厂（create_app）
│   │   │   ├── schemas.py             # Pydantic 请求/响应模型
│   │   │   ├── deps.py                # 依赖注入
│   │   │   └── routes/                # 路由层
│   │   │       ├── auth.py            # POST /auth/token
│   │   │       ├── audit.py           # /audit/start, /status, /approve
│   │   │       └── rag.py             # /rag/upload, /rag/search
│   │   │
│   │   ├── domain/                    # 🧠 领域层（碳审计核心）
│   │   │   ├── calculator.py          # 碳排放计算引擎（Scope 1/2/3）
│   │   │   ├── rag.py                 # RAG 混合检索引擎
│   │   │   ├── report_renderer.py     # Markdown → PDF 渲染
│   │   │   ├── agents/                # 智能体节点
│   │   │   │   ├── router.py          # 🆕 路由 Agent（意图分流）
│   │   │   │   ├── supervisor.py      # 🆕 监督者 Agent（控制循环）
│   │   │   │   ├── data_cleansing.py  # 信息萃取 Agent
│   │   │   │   ├── policy_matcher.py  # 政策判定 Agent (RAG)
│   │   │   │   ├── calculator.py      # 排放计算 Agent
│   │   │   │   ├── compliance_audit.py # 合规风控 Agent
│   │   │   │   └── report_writer.py   # 报告生成 Agent
│   │   │   └── workflows/             # LangGraph 工作流
│   │   │       ├── state.py           # AuditState TypedDict（看板状态）
│   │   │       └── graph.py           # StateGraph 构建（看板+监督者）
│   │   │
│   │   ├── data/                      # 💾 数据访问层
│   │   │   └── database.py            # Milvus + JSON 数据读写
│   │   │
│   │   └── infrastructure/            # 🔧 基础设施层
│   │       ├── cache.py               # Redis 缓存 + 速率限制
│   │       ├── auth.py                # JWT 认证（创建/验证/依赖注入）
│   │       ├── celery_app.py          # Celery 实例配置
│   │       └── celery_tasks.py        # 异步审计任务
│   │
│   ├── tests/                         # 🧪 测试
│   │   ├── conftest.py                # pytest fixtures
│   │   ├── test_calculator.py         # 碳排放计算测试（6 个）
│   │   ├── test_auth.py               # JWT 认证测试（7 个）
│   │   └── test_cache.py              # Redis 缓存测试（5 个）
│   │
│   ├── data/                          # 📦 数据文件
│   │   ├── carbon_db.json             # 碳排放因子数据库
│   │   ├── hs_code_mapping.json       # HS Code 映射表（16 条）
│   │   ├── raw_documents/             # RAG 知识库文档
│   │   │   ├── cbam_regulation_summary.txt
│   │   │   ├── iso_14064_summary.txt
│   │   │   └── us_cca_summary.txt
│   │   └── reports/                   # 生成的审计报告目录
│   │
│   ├── requirements.txt               # Python 依赖清单
│   ├── .env.example                   # 环境变量模板
│   ├── Dockerfile                     # Docker 镜像
│   ├── .dockerignore
│   └── docker-compose.yml             # 多服务编排（4 profile）
│
├── docs/                              # 📖 项目文档
│   ├── 需求文档.md
│   ├── 项目实现计划.md
│   ├── 启动与运维指南.md
│   └── 技术说明文档.md                # ← 本文档
│
├── README.md                          # 项目 README
└── .gitignore
```

### 目录职责速查

| 目录 | 职责 | 改动频率 |
|------|------|----------|
| `frontend/` | Chainlit 对话 UI | 中 |
| `backend/main.py` | 启动入口 | 低 |
| `backend/config/` | 全局配置 | 低 |
| `backend/src/api/` | REST API 接入层 | 中 |
| `backend/src/domain/agents/` | Agent 节点逻辑 | **高** |
| `backend/src/domain/workflows/` | 图编排 + 状态定义 | **高** |
| `backend/src/domain/calculator.py` | 碳排放领域引擎 | 中 |
| `backend/src/domain/rag.py` | RAG 检索引擎 | 中 |
| `backend/src/domain/report_renderer.py` | PDF 渲染 | 低 |
| `backend/src/data/` | 数据访问 | 低 |
| `backend/src/infrastructure/` | 横切基础设施 | 低 |
| `backend/tests/` | 单元测试 | 中 |
| `backend/data/` | 静态数据文件 | 低 |

### 层间依赖关系

```
frontend/app.py ──→ backend/src/domain/ ──→ backend/src/data/
                         │                      │
                         ▼                      ▼
                  backend/src/           backend/data/
                  infrastructure/        (JSON/SQLite/
                      │                 Milvus/Redis)
                      ▼
                  backend/config/
                  settings.py
```

- `frontend/` 依赖 `backend/`（通过 sys.path）
- `api/` 依赖 `domain/` + `infrastructure/`
- `domain/` 依赖 `data/` + `infrastructure/` + `config/`
- `infrastructure/` 依赖 `config/`
- **层间单向依赖，无循环引用**

